<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.bekup_portal.androidlesson3"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".BwkupApplication"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="a362827ddb48f6e7c40545886c7e9639931e3c12"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ListViewActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".RecyclerViewActivity"/>
        <activity android:name=".GetObjectActivity"/>
        <activity android:name=".GetListObjectActivity"/>
        <activity android:name=".GetDataActivity"/>
    </application>

</manifest>